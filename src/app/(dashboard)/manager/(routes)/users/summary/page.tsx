import { StatisticsManagementShell } from '@/features/user-management/components/UserManagementShell'

export default function ManagerUserManagement() {
   return <StatisticsManagementShell />   // same component, role-aware rendering inside
}
