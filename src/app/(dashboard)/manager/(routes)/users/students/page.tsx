import { StudentsManagementShell } from '@/features/user-management/components/UserManagementShell'

export default function ManagerUserManagement() {
   return <StudentsManagementShell />   // same component, role-aware rendering inside
}
